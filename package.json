{
  "name": "otodom-scraper",
  "version": "1.0.0",
  "description": "Otodom real estate scraper",
  "main": "server.js",
  "scripts": {
    "preinstall": "npm install -g npm@latest",
    "start": "node server.js",
    "dev": "nodemon server.js",
    "postinstall": "npx playwright install chromium"
  },
  "dependencies": {
    "@playwright/test": "^1.42.1",
    "axios": "^1.6.0",
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "express-rate-limit": "^7.1.5"
  },
  "devDependencies": {
    "nodemon": "^3.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
} 